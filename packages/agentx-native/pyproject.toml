[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "agent-native"
version = "0.1.0"
description = "Agent Native Core"
authors = [{ name = "Agent Team" }]
requires-python = ">=3.11"
dependencies = []

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "agentx_native"
