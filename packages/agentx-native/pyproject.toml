[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "agentx-native"
version = "0.1.0"
description = "AgentX Native Core"
authors = [{ name = "AgentX Team" }]
requires-python = ">=3.11"
dependencies = []

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "agentx_native"
